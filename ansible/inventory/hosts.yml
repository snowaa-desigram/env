all:
  children:
    local:
      hosts:
        laptop:
          ansible_connection: local
          ansible_python_interpreter: auto_silent
    prod:
      hosts: {}
        # server1:
        #   ansible_host: 1.2.3.4
        #   ansible_user: deploy
