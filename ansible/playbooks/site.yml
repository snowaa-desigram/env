# Полный разворот: (bootstrap уже сделан) -> hardening актуален -> docker -> код -> стек
- name: Provision servers
  hosts: prod
  become: true
  roles:
    - server
    - docker

- name: Deploy application
  hosts: prod
  roles:
    - app
